language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-vile' ]; then cd .. && eval "mv $currentfolder generator-vile" && cd generator-vile; fi

